@model IEnumerable<GoalGetters.Models.Team>

@* <p>
    <a asp-action="Create">Create New</a>
</p> *@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Country)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var team in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => team.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => team.Country)
            </td>
             <td>
                @Html.ActionLink("Edit", "EditTeam", new { id=team.Id , entity="player"}) |
                    @Html.ActionLink("Details", "DetailsTeam", new { id = team.Id }) |
                    @Html.ActionLink("Delete", "DeleteTeam", new { id = team.Id })
            </td> 
        </tr>
}
    </tbody>
</table>
